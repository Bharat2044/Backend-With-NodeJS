// This module.exports statement is used to export an object containing properties.
module.exports = {

    // 'secret' is a key in the exported object, and its value is a string.
    // This string represents a secret key or passphrase. It is important to keep
    // such sensitive information secure and not expose it in your code repository.
    secret: "This is my super private secret"
}
